{
  "name": "neko-front",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "sass": "^1.26.2"
  },
  "scripts": {
    "serve": "parcel src/index.html -p 8000",
    "build": "rm dist/* && parcel build src/index.html --out-dir dist/"
  }
}
